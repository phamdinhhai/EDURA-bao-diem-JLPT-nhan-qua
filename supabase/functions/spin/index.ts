import { serve } from 'https://deno.land/std@0.224.0/http/server.ts';
import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};
serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});try{const body=await req.json();const participant=body.participant||{};if(body.campaign!=='JLPT_SPIN_2026'||!body.request_id||!participant.name||!participant.phone)return json({code:'INVALID_PAYLOAD'},400);if(!/^(\+84|0)(9|8|7|5|3)[0-9]{8}$/.test(String(participant.phone).replace(/\s/g,'')))return json({code:'INVALID_PHONE'},400);const supabase=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const {data,error}=await supabase.rpc('allocate_spin',{p_campaign:body.campaign,p_name:String(participant.name).trim().slice(0,80),p_phone:String(participant.phone).replace(/\s/g,''),p_request_id:body.request_id});if(error){const message=error.message||'';const code=['CAMPAIGN_INACTIVE','ALREADY_SPUN','CAMPAIGN_SOLD_OUT'].find(x=>message.includes(x))||'SPIN_FAILED';return json({code},code==='SPIN_FAILED'?500:409)}return json(Array.isArray(data)?data[0]:data,200)}catch{return json({code:'INVALID_REQUEST'},400)}});
function json(body:unknown,status:number){return new Response(JSON.stringify(body),{status,headers:{...cors,'Content-Type':'application/json'}})}
